{
  "name": "chatgpt-app-mcp-server",
  "version": "1.0.0",
  "description": "MCP Server for fetching product offers from the Marketplace (prototype)",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "node --env-file=.env --import=tsx src/index.ts",
    "dev:http": "node --env-file=.env --import=tsx src/server.ts",
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "axios": "^1.7.9",
    "express": "^4.21.2",
    "zod": "^3.23.8",
    "zod-to-json-schema": "^3.23.5"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}